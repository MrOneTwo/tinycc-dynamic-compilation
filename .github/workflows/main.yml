name: build
on:
  push:
    branches: [ master ]
jobs:
  fetch-toolchain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd $GITHUB_WORKSPACE && make && timeout 1 ./main target.c
